```typescript
/**
 * Module for handling click outside events.
 */
import clickOutside from './src/clickOutside';

/**
 * Module for checking if an element fits below another.
 */
import fitsBelow from './src/fitsBelow';

/**
 * Module for focusing on an element.
 */
import focus from './src/focus';

/**
 * Module for syntax highlighting using the highlight.ts library.
 */
import hljs from './src/hljs';

/**
 * Module for handling long click events.
 */
import longClick from './src/longClick';

/**
 * Module for handling element resize events.
 */
import resize from './src/resize';

/**
 * Module for selecting all text inside an input element on focus.
 */
import selectOnFocus from './src/selectOnFocus';

/**
 * Exported modules for external use.
 */
export {
    clickOutside,
    fitsBelow,
    focus,
    hljs,
    longClick,
    resize,
    selectOnFocus,
};
```